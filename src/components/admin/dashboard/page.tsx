// Местоположение: src/components/admin/dashboard/page.tsx (ОБНОВЛЕННЫЙ)

// Теперь можно импортировать так:
import { ProductTable, DashboardControls } from '@/components/admin';
import { ADMIN_ROUTES, PRODUCT_TABLE_CONFIG } from '@/lib/constants/admin';
import { formatPrice, formatDate } from '@/lib/utils/formatting';
import { 
  CartIcon, 
  SearchIcon, 
  SettingsIcon 
} from '@/components/shared/icons';

// Остальной код без изменений...

